pubmed:
  email: "your_email@example.com"
  searches:
    - query: '("Radiology") AND (("large language model") OR ("natural language processing") OR ("GPT"))'
      sheet_name: "Radiology NLP"
    - query: '("CT") AND ("deep learning")'
      sheet_name: "CT Deep Learning"
    - query: '("MRI") AND ("artificial intelligence")'
      sheet_name: "MRI AI"

sheets:
  spreadsheet_id: "YOUR_SHEET_ID"

filters:
  high_if_journals:
    # General High-Impact
    - "Nature"
    - "Science"
    - "Cell"
    - "Nature Medicine"
    - "The Lancet"
    - "New England Journal of Medicine"
    - "JAMA"
    # Radiology
    - "Radiology"
    - "European Radiology"
    - "American Journal of Roentgenology"
    - "Radiographics"
    - "Journal of Nuclear Medicine"
    # AI & Medical Imaging
    - "Nature Machine Intelligence"
    - "IEEE Transactions on Pattern Analysis and Machine Intelligence"
    - "IEEE Transactions on Medical Imaging"
    - "Medical Image Analysis"
    - "Artificial Intelligence in Medicine"

# LLM Configuration (Optional: Customize prompts and model settings)
# Uncomment and modify to override defaults
# llm:
#   model: "gpt-4o-mini"
#   temperature: 0.2
#
#   # Novelty evaluation prompt
#   # Available variables: {title}, {journal}, {abstract}
#   novelty_prompt: |
#     Determine whether the paper demonstrates genuinely novel, cutting-edge methodology.
#     Only mark as novel if the contribution is clearly new and meaningfully distinct from existing work.
#     Title: {title}
#     Journal: {journal}
#     Abstract: {abstract}
#
#   # Summary generation prompt
#   # Available variables: {title}, {journal}, {abstract}
#   summary_prompt: |
#     Summarize the abstract in no more than 3 lines and describe why it is strong or impactful.
#     Title: {title}
#     Journal: {journal}
#     Abstract: {abstract}
